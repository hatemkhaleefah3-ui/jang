# Reference design coverage

Generation validates and reuses the approved reference HTML design system before building a lecture. The recognized catalog includes font and color tokens, heading and paragraph styles, forms and callouts, tables, image layouts, diagram families, page headers and footers, cover structure, standard pages, and the end page. Missing required tokens, classes, or templates stop generation instead of silently inventing replacements.
