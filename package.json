{
  "name": "jang-lecture-html-builder",
  "version": "3.0.0",
  "private": true,
  "description": "Paste or import lecture text and download a responsive standalone HTML lecture.",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check lecture-html.js && node --check lecture-source-parser.js && node --check scripts/build.mjs && node --check scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "unit": "node --test tests/unit/lecture-source-parser.test.mjs tests/unit/lecture-html.test.mjs",
    "test": "npm run check && npm run unit && npm run build"
  }
}
