# Responsive layout contract

Editable visual components store width as a percentage of the document flow container. Left and right alignment use normal float-based reflow so nearby text expands into freed space when a component is reduced and yields space when it is enlarged. Responsive typography uses bounded viewport scaling, and pages switch to single-column behavior where narrow screens cannot preserve readable wrapped text.
