{
  "name": "jang-lecture-rebuilder",
  "version": "2.2.1",
  "private": true,
  "description": "Browser-first PPTX, PDF, and HTML lecture redesign tool deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-07-23",
    "check": "node --check app-v2.js && node --check extractor-v2.js && node --check source-importer.js && node --check ocr-engine.js && node --check fallback-plan.js && node --check pptx-exporter.js && node --check pptx-exporter-v2.js && node --check lecture-template.js && node --check functions/api/config.js && node --check functions/api/redesign.js && node --check functions/api/redesign-large.js && node --check scripts/build.mjs && node --check tests/create-fixtures.mjs",
    "build": "node scripts/build.mjs",
    "unit": "node --test tests/unit/fallback-plan.test.mjs tests/unit/pptx-layout.test.mjs tests/unit/source-fidelity.test.mjs",
    "test:unit": "npm run unit",
    "test:fixtures": "node tests/create-fixtures.mjs",
    "test:e2e": "npm run build && npm run test:fixtures && playwright test",
    "test": "npm run check && npm run unit && npm run test:e2e"
  },
  "dependencies": {
    "@tesseract.js-data/ara": "1.0.0",
    "@tesseract.js-data/eng": "1.0.0",
    "emf-converter": "2.0.0",
    "jszip": "3.10.1",
    "pdfjs-dist": "4.10.38",
    "pptxgenjs": "3.12.0",
    "tesseract.js": "7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "pdf-lib": "1.17.1",
    "wrangler": "^4.25.0"
  },
  "type": "module"
}
