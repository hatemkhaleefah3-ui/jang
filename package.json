{
  "name": "jang-lecture-rebuilder",
  "version": "2.2.0",
  "private": true,
  "description": "Browser-first PPTX, PDF, and HTML lecture redesign tool deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-07-23",
    "check": "node --check app-v2.js && node --check extractor-v2.js && node --check source-importer.js && node --check fallback-plan.js && node --check pptx-exporter.js && node --check lecture-template.js && node --check functions/api/config.js && node --check functions/api/redesign.js && node --check functions/api/redesign-large.js && node --check scripts/build.mjs && node --check tests/create-fixtures.mjs",
    "build": "node scripts/build.mjs",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:fixtures": "node tests/create-fixtures.mjs",
    "test:e2e": "npm run build && npm run test:fixtures && playwright test",
    "test": "npm run check && npm run test:unit && npm run test:e2e"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "pdfjs-dist": "6.1.200",
    "pptxgenjs": "3.12.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "pdf-lib": "1.17.1",
    "wrangler": "^4.25.0"
  },
  "type": "module"
}
