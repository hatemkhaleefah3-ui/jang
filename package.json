{
  "name": "jang-lecture-rebuilder",
  "version": "1.1.0",
  "private": true,
  "description": "Browser-first lecture HTML redesign tool deployed on Cloudflare Pages.",
  "scripts": {
    "dev": "npm run build && wrangler pages dev dist --compatibility-date=2026-07-23",
    "check": "node --check app-v2.js && node --check extractor-v2.js && node --check lecture-template.js && node --check functions/api/config.js && node --check functions/api/redesign-large.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  },
  "type": "module"
}
