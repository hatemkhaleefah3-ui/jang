# Project persistence contract

The builder stores generated project HTML, source text, verification metadata, title, and save timestamp in browser local storage. Saved projects can be reopened and deleted from the builder. The standalone HTML also contains its own editing toolbar and can save its edited DOM locally or download the complete edited document.
