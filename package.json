{
  "name": "jang-lecture-pptx-builder",
  "version": "5.0.0",
  "private": true,
  "description": "Import a PDF or PPTX lecture, structure it with Gemini, add recognized images, and download an editable PowerPoint presentation.",
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check pptx-output.js && node --check pptx-engine.js && node --check pptx-reader.js && node --check functions/api/extract.js && node --check scripts/build.mjs && node --check scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "unit": "node --test tests/unit/extract-function.test.mjs tests/unit/pptx-workflow.test.mjs",
    "test": "npm run check && npm run unit && npm run build",
    "sample": "node scripts/generate-sample.mjs"
  }
}
