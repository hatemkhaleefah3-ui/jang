# Content fidelity contract

The source string is retained verbatim. Structural marker lines determine component type, but lecture content is not summarized, paraphrased, translated, corrected, merged, split, or reordered. The generated document is rejected unless every source block ID appears exactly once and in the original sequence.
