{
  "name": "jang-lecture-html-builder",
  "version": "3.0.0",
  "private": true,
  "description": "Paste or import lecture text and download one responsive HTML slide document.",
  "type": "module",
  "scripts": {
    "check": "node --check builder-app.js && node --check lecture-source-parser.js && node --check lecture-html.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs",
    "unit": "node --test tests/unit/*.test.mjs",
    "test": "npm run check && npm run unit && npm run build"
  }
}
